[build]
# constant-time 라이브러리 자체는 no_std를 유지하지만,
# 테스트 바이너리는 표준 하네스(std)가 필요하므로 호스트 타깃 사용
# 워크스페이스 루트의 target = "x86_64-unknown-none" 설정을 이 크레이트에서 재정의함
# 의존성으로 빌드될 때는 상위 크레이트의 타깃을 따르기 떄문에 커널 빌드에 영향 없음
target = "x86_64-apple-darwin"
